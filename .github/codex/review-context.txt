# Контекст ревьюера: только контракты и глобальные инварианты.
# Список и содержимое читаются из merge-base PR; правка этих файлов в самом
# PR видна как предложение, но не может переписать правила своего ревью.
# По одному буквальному пути на строку, без glob-ов и пробелов.

docs/review-context/behaviour-bundle-tasks-contract.md
spec/FORMAT.md
