---
spec_stage: requirements
status: draft
owner_role: product
traces_to:
- charter
---

# Requirements — Исполняемый negative control под `Mode: standard` (spec-runner#428)

## 1. Постановка

Класс waiver'а `characterisation` снимает с задачи обязанность показать
baseline RED. Взамен он требует одного: **negative control — доказательство,
что новый тест краснеет при нарушении свойства, которое он объявляет
проверять.** Сегодня это требование не исполняется, а декларируется: пункт
чек-листа отмечает сам исполняющий агент
(`spec/verify-first-file-scope-group-targets-20260908-tasks.md:356`), а
абзац для ревьюера заканчивается признанием «no gate anywhere checks this —
you are the only check» (`config.py:96`).

Стадия переводит требование в исполняемую форму: задача объявляет мутант и
селектор, харнесс воспроизводит обе половины против коммита кандидата,
результат записывается и читается, а задача без удовлетворённого контроля
не завершается.

Граница зафиксирована и не расширяется: машина доказывает, что тест
различает **приложенный** мутант. Соответствие мутанта заявленному свойству
остаётся предметом ревью и прямо называется в тексте обязательства (FR-08).

## 2. Цели, персоны, jobs

### Цели

- **G-01** — Ни одна waived-задача класса `characterisation` не достигает
  терминального завершения без исполненного негативного контроля.
- **G-02** — Результат контроля — durable-факт, читаемый позже и из одного
  источника обеими поверхностями (`tdd status`, `--json`).
- **G-03** — Сужение работы ревьюера названо вслух: он проверяет
  соответствие мутанта свойству, а не наличие доказательства.
- **G-04** — Механика дормантна: проект без waived-задач не платит за неё
  ничем.

### Персоны

- **P-01 Владелец репо** — принимает waived-задачи; сегодня доверяет
  отметке исполнителя.
- **P-02 Ревьюер задачи** — сегодня единственная проверка условия.
- **P-03 Исполняющий агент** — сторона, чьё «честное слово» заменяется
  машинной проверкой.
- **P-04 Мост devtools** (#197) — потребитель класса waiver'а.

### Jobs-to-be-done

- **J-01** (P-01) «Убедиться, что закрытая waived-задача действительно
  принесла тест, способный покраснеть» — сегодня невозможно иначе как
  чтением диффа.
- **J-02** (P-02) «Понять, что именно от меня требуется, когда машина уже
  что-то проверила» — сегодня текст обязательства описывает мир без гейта.
- **J-03** (P-03) «Узнать до платного вызова, что моё объявление негодно» —
  сегодня негодность объявления вообще не определена.
- **J-04** (P-01) «Не платить за механику на проекте, где waiver'ов нет».

## 3. Термины

- **Мутант** — патч в формате `git apply`, лежащий в дереве задачи по пути
  `spec/negative-controls/<TASK-ID>.patch`, ломающий свойство, которое
  проверяет тест задачи.
- **Кандидат-коммит** — коммит, который пре-терминальный сайт получает как
  `candidate_sha` и который несёт и новый тест, и файл патча
  (`hooks.py:1166` делает его до гейтов).
- **Чистая половина** — прогон объявленного селектора против кандидат-коммита
  **без** применения патча. Исполняется первой.
- **Мутированная половина** — прогон **того же** селектора против **того же**
  коммита с применённым патчем.
- **Прогон не состоялся** — тест не был исполнен вовсе: отказ до
  исполнения, preflight, подготовка окружения, таймаут, исключение.
- **Прогон без вердикта** — тест исполнялся, но результат не читается как
  вердикт.

  Оба термина описывают **исход**, а не его классификацию. Чей это отказ —
  стенда или патча — решает свойство FR-04; конкретное отображение
  источников и кодов на вердикты принадлежит стадии design и живёт там,
  чтобы не существовать в двух редакциях сразу.
- **Красный по ожидаемой причине** — `RunOutcome.TESTS_FAILED` вместе с
  `SelectionProof.PROVEN` (`tdd.py:447`). Определение репо, не новое.

## 4. Функциональные требования

#### FR-01: Форма объявления — отдельный маркер, два обязательных поля

**Priority**: Must

Задача, объявившая `**TDD-waiver:** characterisation`, обязана нести
отдельную строку:

```
**Negative-control:** <путь к патчу> :: <селектор>
```

Грамматика `**TDD-waiver:**` остаётся из двух полей (класс, санкция) и не
трогается.

Уточнения:

- Разделитель — ` :: `, двоеточия **с пробелами по обе стороны**. Это
  снимает неоднозначность с `::` внутри pytest-селектора
  (`tests/test_x.py::test_y`), где пробелов нет.
- Строка делится по ` :: ` **ровно один раз**. Ноль вхождений — объявление
  неполно; больше одного — неоднозначно. Оба случая — отказ, а не выбор
  одной из интерпретаций.
- Оба поля обязательны. Пустой путь, пустой селектор, отсутствующая строка —
  негодное объявление.
- Селектор **один на обе половины**: это одно объявление, а не два.
- Маркер у задачи **без** waiver'а — тоже дефект объявления: контроль без
  снятой обязанности не имеет предмета.
- **Наличие и разбираемость объявления проверяются до платного вызова** —
  там же, где waived-задачу уже проверяет гейт claims (точка 1,
  `execution.py:656`). Откладывать этот отказ до пре-терминального сайта
  значит жечь полный платный вызов на каждом возобновлении ради факта,
  известного из `tasks.md` на старте — форма, которой репо избегает
  намеренно (`hooks.py:1176`). Это обязанность **Must**, и она живёт
  здесь, а не внутри Should-требования про `validate`: стадия, отбирающая
  Must, иначе законно прошла бы мимо неё.

**Acceptance**:

- Разобранная задача несёт путь и селектор раздельно; ни одно поле не
  склеено с другим.
- `**Negative-control:** a.patch :: b.patch :: tests/t.py::x` — отказ по
  неоднозначности, с названием причины.
- `**Negative-control:** tests/t.py::x` (нет разделителя) — отказ по
  неполноте.
- `**Negative-control:**` у задачи без `**TDD-waiver:**` — названная ошибка.
- Задача без waiver'а и без маркера разбирается ровно как сегодня —
  байт-в-байт тот же `Task`.
- Waived-задача с негодным или отсутствующим объявлением отказана **до**
  первого платного вызова, а не на пре-терминальном сайте.

traces: [G-01, J-03, P-03, AP-10]

#### FR-02: Контроль исполняет харнесс, чистая половина первой

**Priority**: Must

Харнесс воспроизводит контроль сам, против кандидат-коммита, в одноразовом
`git worktree --detach`, по образцу и с дисциплиной `verify_red`
(`tdd.py:270`): preflight по дереву **того** коммита, изоляция окружения,
удаление worktree и приватной сборки на **каждом** пути.

Порядок фиксирован: **сначала чистая половина, затем мутированная.**
Чистая половина служит доказательством исправности инструмента: `PROVEN` +
`TESTS_PASSED` означает, что селектор разрешается, раннер работает и
окружение собралось. Без этого доказательства мутированная половина не
исполняется вовсе.

Ни один платный вызов при этом не совершается: контроль — воспроизведение,
а не агент.

**Acceptance**:

- Обе половины исполняются на настоящем репозитории с настоящим раннером;
  worktree после прогона не остаётся ни на одном пути, включая исключение и
  таймаут.
- Мутированная половина не запускается, если чистая не дала
  `PROVEN` + `TESTS_PASSED`.
- Ни один тест механики не поднимает `PaidBinaryReached` и не обходит пояс.

traces: [G-01, AP-08, S-03, NFR-01]

#### FR-03: Красный засчитывается только по ожидаемой причине

**Priority**: Must

Мутированная половина засчитывается красной **только** при
`RunOutcome.TESTS_FAILED` вместе с `SelectionProof.PROVEN`. Мутант,
уронивший сбор, компиляцию или соседний тест, красным не считается.

Второго определения «ожидаемой причины» не заводится: используется
существующее (`tdd._classify`). Два определения в одном репо — источник
расхождения того же класса, который #270 чинил для вердиктов ревью.

**Acceptance**:

- Патч, роняющий сбор, не даёт удовлетворённого гейта.
- Патч, роняющий **другой** тест при зелёном объявленном, не даёт
  удовлетворённого гейта (`PROVEN` отвечает именно на этот вопрос).
- В свидетельстве видно, чем кончилась каждая половина.

traces: [G-01, S-04, инвариант 2]

#### FR-04: Вина патча и вина стенда различаются, и различение доказуемо

**Priority**: Must

Требование — **свойство**, не таблица отображений. Таблица (какой источник
и какой код отказа куда) принадлежит стадии design и пишется там один раз
против кода; здесь фиксируется то, чему она обязана удовлетворять.

1. **Невозможность проверить никогда не читается как отказ по существу.**
   Отказ, вызванный стендом — исчезнувший тулчейн, таймаут, неподготовленное
   окружение, сорвавшаяся подготовка worktree, — остаётся
   `instrument_error` со своими ретраями. Это не смягчение, а действующее
   правило репо: `gates.py:414` уже отображает неподтверждённый исход
   именно так, и второе, расходящееся отображение того же значения есть
   ровно то, что запрещает инвариант 2.
2. **Отказ, вызванный содержимым патча, — отказ по существу**, без ретраев.
   Иначе авторская ошибка приходит оператору как «сломан харнесс» и жжёт
   весь бюджет восстановления на детерминированном событии — то, что FR-05
   отвергает для красной чистой половины, и здесь оно так же неверно.
3. **Зелёная чистая половина доказывает исправность стенда в тот момент, а
   не на следующий прогон.** Она сужает круг подозреваемых, но не делает
   всякий последующий отказ виной патча.
4. **При сомнении — instrument.** Ложно обвинить автора хуже, чем лишний
   раз переспросить инструмент: первое отправляет человека чинить верный
   патч, второе стоит прогона.

**Доказуемость — часть требования.** Сегодня различение недоступно вызывающему:
`verify_red` сводит любую причину к одному значению `UNVERIFIABLE`, сохраняя
лишь текст сообщения (`tdd.py:363`–`:378`, `:456`). Значит design обязан
предъявить шов, который доносит причину до классификатора; без него
свойства 1–2 нереализуемы, а не просто неудобны. Прецедент несущего поля
рядом: `RedPhaseResult(..., instrument_error=...)` (`tdd.py:855`).

**Расхождение с AP-08 объявлено, а не сделано молча.** AP-08 (решение
владельца, круг 1) относил preflight-отказ к классу «прогон не состоялся»
и оставлял его instrument error даже после зелёной чистой половины.
Последующие круги показали, что на ExUnit именно preflight ловит
патч-обусловленное повреждение тест-файла, и буквальное AP-08 сделало бы
приёмку ниже недостижимой на одном из двух измеренных раннеров. Требование
здесь **заменяет** AP-08 в этой части: вердикт определяется причиной
отказа, а не стадией. Помечено как ожидающее подтверждения владельца — по
образцу Q-F, а не как тихая правка решения.

**Acceptance** — наблюдаемая снаружи, без ссылок на внутренние коды:

- Патч, ломающий тест-файл так, что названный тест не может быть исполнен:
  гейт не удовлетворён, ретраев нет. Проверяется на **обоих** измеренных
  раннерах: механизм отказа у них разный, вердикт обязан быть один.
- Тулчейн раннера недоступен на мутированной половине при зелёной чистой:
  instrument error с ретраями, а не «мутант не различил».
- Таймаут реплея и сорвавшаяся подготовка окружения на мутированной
  половине: instrument error с ретраями.
- Ни один из перечисленных исходов не записывается как успешный контроль.

traces: [G-01, AP-08, CON-05, инвариант 4]

#### FR-05: Чистая половина — два вердикта по существу, остальное инструмент

**Priority**: Must

Исходы чистой половины различаются по существу:

- **`PROVEN` + `TESTS_PASSED`** — инструмент исправен, контроль продолжается.
- **`PROVEN` + `TESTS_FAILED`** (`_classify` → `EXPECTED_FAIL`,
  `tdd.py:447`) — **гейт не удовлетворён, без ретраев**. Тест, красный без
  всякого мутанта, — то самое мошенничество, ради которого требование обеих
  половин существует; это факт о тесте задачи, а не о харнессе.
- **Всё остальное** — не состоявшийся прогон и прогон без вердикта —
  `instrument_error` с ретраями. Патча на этой половине ещё нет,
  приписывать ему нечего, а обвинить тест задачи означало бы назвать
  исправный тест негодным по причине, которой в нём может не быть вовсе:
  правило «при сомнении — instrument» (FR-04) решает и этот случай.

Классификация «красный без мутанта» как instrument error запрещена: она
говорит оператору «харнесс сломался» про исправный харнесс и сжигает весь
бюджет восстановления на детерминированной ошибке каждую попытку.

**Acceptance**:

- Тест, красный на чистом кандидат-коммите: гейт не удовлетворён, ретраев
  нет, сообщение говорит о тесте, а не об инструменте.
- Селектор, не найденный в дереве коммита: instrument error.
- Нечитаемый файл патча (I/O): instrument error с названной причиной и без
  записи об успехе. Нераспознанный раннер и композитный `test_command` сюда
  **не входят**: они известны статически и отвергаются до платного вызова
  (FR-09).
- Патча **нет в кандидат-коммите**: не удовлетворён, без ретраев —
  отсутствие продукта задачи, а не сбой инструмента (допущение Q-F, §10).
- **`git apply` отказал** — предмет Q-F: AP-02 говорит instrument error, но
  применимость детерминирована (инвариант 5), и ретраи прогоняют чистую
  половину заново на авторской ошибке. Записано по AP-02 буквально до
  ответа владельца.

traces: [G-01, AP-09, инварианты 3 и 4]

#### FR-06: Обе половины записываются durable

**Priority**: Must

Записывается то, что делает запись читаемой позже: идентификатор задачи и
неймспейс, кандидат-коммит, селектор, blob-хэш патча, исход **каждой**
половины, идентичность окружения и конфигурации, отметка времени.

Запись появляется в `tdd status <TASK-ID>` и в `--json` **из одного чтения**
(`tdd_status.collect`) — текст и JSON не могут разойтись.

**Acceptance**:

- После прогона обе половины видны в обеих поверхностях, и значения
  совпадают.
- Неудача записи не роняет прогон и не выдаётся за успех контроля.
- Запись другой задачи или другого неймспейса не читается как своя.

traces: [G-02, IF-03, инвариант 7]

#### FR-07: Fail-closed, отказ резюмируемый

**Priority**: Must

Waived-задача без удовлетворённого контроля терминального завершения не
получает. Отказ переиспользует существующий путь «попытка не закончилась»:
задача остаётся возобновляемой, кандидат-коммит остаётся на месте, своего
терминального состояния гейт не заводит.

**Вторая структурная невозможность, названная по имени.** Гарантия выше
опирается на пре-терминальный сайт, а он **пропускает все гейты**, если
кандидат-коммит не резолвится: `_head_sha` возвращает пустую строку при
любом ненулевом коде `git rev-parse HEAD`, и `_run_pre_terminal_gates`
тогда логирует предупреждение и возвращает `None` — то есть каждый
зарегистрированный гейт считается пройденным (`hooks.py:573`, `:380`).
Waived-задача получила бы терминальное завершение без единого исполненного
контроля. Поэтому: **не резолвящийся кандидат-коммит у waived-задачи —
отказ, а не пропуск**, по тому же стандарту, по которому FR-09 называет
отсутствие `auto_commit`.

**Acceptance**:

- Задача с негодным контролем остаётся возобновляемой; повторный прогон
  после исправленного патча проходит.
- Waived-задача, у которой кандидат-коммит не резолвится, отказана, а не
  пропущена; не-waived задачи того же прогона не затронуты.
- Отказ приходит типизированным (`Refusal`), а не строкой, и классифицируется
  одним местом (`refusal_for`).

traces: [G-01, S-06, AP-03, AP-04]

#### FR-08: Текст обязательства ревьюеру переписан под новую границу

**Priority**: Must

`WAIVER_REVIEW_OBLIGATIONS["characterisation"]` больше не утверждает «you
are the only check». Текст говорит: машина показала, что тест различает
приложенный мутант; предмет ревью — **соответствие мутанта заявленному
свойству**, поскольку патч может ломать что угодно в том же файле.

Это Must, а не Should: гейт, закрывающий половину вопроса, легко читается как
закрывший вопрос, и оставленный старый текст сделал бы ревьюера менее
внимательным ровно там, где его внимание остаётся единственным.

**Acceptance**:

- Фраза «you are the only check» отсутствует.
- Текст называет мутант и предмет ревью; пинуется тестом.
- Текст по-прежнему приходит из КЛАССА, а не из слов маркера.

traces: [G-03, P-02, J-02, CON-01, инвариант 9]

#### FR-09: Структурная невозможность — названный отказ до платного вызова

**Priority**: Must

Конфигурация, при которой негативный контроль **не может быть исполнен в
принципе**, отвергается по имени и потасково, у задачи, которая идёт
исполняться, до платного вызова. Не ретраится как instrument error и не
откладывается до пре-терминального сайта.

Требование сформулировано **классом, а не перечнем случаев** (круг 10): все
они приходят к одному и тому же вреду, и чинить их поодиночке значит ждать
следующего. Вред: гейт отвечает instrument error, тот ретраится
`gate_recovery_attempts` раз с одним и тем же детерминированным ответом,
после чего отдаётся как infrastructure — задача **не завершается никогда**,
сколько бы верных патчей автор ни приложил, оператор читает «сломан
харнесс» про исправный харнесс, а каждое возобновление стоит полного
платного вызова, потому что единственная точка оценки стоит после него.
Это дословно «тихая невозможность», запрещённая инвариантом 8.

Члены класса, известные сегодня, — все устанавливаются **статически, из
конфига и объявления**:

- `auto_commit: false` — кандидат-коммита не существует вовсе
  (`hooks.py:1166`), и судить было бы нечего;
- адаптер не резолвится — `resolve_tdd_runner` без объявленного
  `tdd_runner` возвращает результат `infer_adapter` (`config.py:893`), а тот
  отдаёт `None` для всего вне `ADAPTERS` (`tdd_runners.py:1586`); `standard`
  такой проект принимает, а контроль исполнить нельзя;
- композитный `test_command` — его нельзя сузить до одного селектора, и
  `verify_red` отказывается угадывать (`tdd.py:284`).

Перечень открыт по построению: критерий членства — «известно до прогона и
делает контроль неисполнимым», а не длина списка.

Прецедент формы — #380 круга 4: ровно вопрос про `auto_commit` был перенесён
в потасковый отказ до платного вызова, `_run_verify_first_phase`
(`execution.py:450`: `if not config.auto_commit` → терминальный `Refusal`).
Прецедент, которым НЕ надо пользоваться, — `required` + `run_review: false`:
там противоречие конфига с конфигом, а waiver — факт задачи в `tasks.md`, и
конфиг-уровневая проверка отравила бы проект навсегда закрытой
waived-задачей (три таких `✅ DONE` в этом же репо), что запрещает OUT-03.

**Acceptance**:

- Каждый член класса даёт waived-задаче названный отказ до первого платного
  вызова; ошибка называет обе стороны противоречия.
- Ни один из них не приводит к ретраям гейта и к infrastructure-исходу.
- Не-waived задачи того же проекта исполняются как обычно.
- Закрытая (`done`) waived-задача не блокирует ни `validate`, ни прогон.

traces: [G-01, CON-04, CON-06, инвариант 8]

#### FR-10: `validate` сообщает о дефектном объявлении статически

**Priority**: Should

До всякого прогона `validate` называет дефекты объявления — **но только у
задач, которые ещё пойдут исполняться**, и **только те факты, которые
статическая проверка вправе считать установленными**.

Две границы, и обе обязательны, иначе требование ломает то, что обещает.

**Граница 1 — по статусу задачи.** Проверка применяется к задачам, не
достигшим `done`. Закрытая waived-задача не даёт ни ошибки `validate`, ни
блокировки прогона. Без этой оговорки три `✅ DONE` задачи этого же репо
(TASK-008/009/011, несущие `**TDD-waiver:**` и не несущие
`**Negative-control:**`, которого тогда не существовало) сделали бы
`pre_result.ok` ложным, и `run`/`watch` не стартовали бы вовсе
(`cli.py:904`, `:1560`) — тот самый ретроспективный эффект, который
запрещает OUT-03 и которому противоречит приёмка FR-09.

**Граница 2 — по дереву, которое `validate` вправе судить.** Отсутствие
файла патча в **рабочем дереве** — **предупреждение, не ошибка**. Патч
создаётся исполнением задачи: §3 определяет его как файл, который несёт
кандидат-коммит, а мера RK-01 требует, чтобы он лежал в дифе задачи. До
первого прогона его нет по построению, и ошибка здесь сделала бы новую
waived-задачу незапускаемой навсегда: `validate` не пускает прогон →
задача не исполняется → патч не пишется → `validate` снова не пускает.
Репо уже решило ровно этот вопрос в другую сторону для verify-first
(`validate.py:765`): объявленный, но отсутствующий файл — warning, потому
что «`validate` judges the working tree, not the commit a live run will
replay». Блокирующим фактом остаётся отсутствие патча в **кандидат-коммите**.

**Чем именно он блокирует — сказано явно, а не отдано исполнению.**
«Его ловит исполнение контроля» было бы недостаточно: instrument error по
инварианту 4 не читается как отказ по существу, и «блокирующий факт»
обеспечивался бы механизмом, который бандл сам объявил не-вердиктом. А это
не редкий угол: забытый или незакоммиченный патч — самая частая авторская
ошибка, детерминированная, ровно того класса, которому FR-05 отказывает в
instrument-классификации. Поэтому отсутствие патча в кандидат-коммите —
**не удовлетворён, без ретраев** (допущение Q-F, §10).

Ошибками `validate` остаются дефекты, установимые **из самого объявления**:
отсутствующий маркер у незакрытой waived-задачи, неразбираемая строка (ноль
или больше одного ` :: `), пустое поле, маркер у не-waived задачи.

**Acceptance**:

- Каждый дефект объявления — названная ошибка `validate`, а не traceback.
- Задача со статусом `done`, несущая waiver и не несущая
  `**Negative-control:**`, не даёт ни ошибки, ни предупреждения, и не
  блокирует прогон соседних задач.
- Объявленный патч, отсутствующий в рабочем дереве, — предупреждение;
  `pre_result.ok` остаётся истинным, и прогон стартует.

traces: [G-01, J-03, S-01, OUT-03]

#### FR-11: Контракт синхронизирован

**Priority**: Should

`docs/state-schema.md` описывает новую таблицу; CHANGELOG несёт запись;
#428 закрыт с evidence; мосту devtools отправлен issue (правки в его репо
нет — граница не переопределяется).

**Acceptance**:

- Перечисленное присутствует в PR.
- В соседнем репозитории не изменено ни одного файла.

traces: [G-02, P-04, IF-05, OUT-01]

#### FR-12: Прогон контроля по требованию

**Priority**: Could

Автор задачи может прогнать контроль, не доводя задачу до терминального
сайта, и получить тот же вердикт, который увидит гейт.

**Acceptance**:

- Команда прогоняет обе половины и печатает вердикт, совпадающий с гейтовым.
- Команда не пишет свидетельства, которое гейт потом прочитает как своё
  (иначе она становится способом обойти гейт).

traces: [J-03, P-03]

## 5. Нефункциональные требования

- **NFR-01 · safety** — Ни одного платного вызова. *Приёмка:* пояс
  `PaidBinaryReached` не обходится ни одним тестом механики.
- **NFR-02 · cost** — Полная дормантность. Задача без waiver'а не платит ни
  git-вызовом, ни прогоном тестов. Регистрация гейта — **потасковая**, по
  образцу обёртки `execute_task` (`borrowed` + detach в `finally`,
  `execution.py:584`): оба проектных сайта регистрации `tasks.md` не читают,
  и регистрация там включила бы кандидат-коммит для всех задач прогона.
  *Приёмка:* прогон проекта без waived-задач не отличается от сегодняшнего
  по числу git-вызовов и открытий state DB.
- **NFR-03 · reliability** — Worktree и приватная сборка удаляются на каждом
  пути; утёкший worktree ломает следующий `git worktree add`. *Приёмка:*
  переиспользуется существующий `finally` из `verify_red`, своего управления
  временем жизни не заводится.
- **NFR-04 · cost** — Стоимость контроля ограничена: два прогона селектора на
  waived-задачу, каждый под тем же таймаутом, что реплей RED. *Приёмка:*
  мутированная половина не запускается без зелёной чистой (FR-02), то есть
  негодный стенд стоит один прогон, а не два.

## 6. Ограничения

- **CON-01** Машина доказывает различение против приложенного мутанта, не
  соответствие мутанта свойству. Остаток — ревьюеру (FR-08).
- **CON-04** Только измеренные раннеры: `verify_red` отказывается
  подтверждать красный там, где карта кодов возврата не измерена
  (`tdd.py:300`). Контроль наследует это — но **не молча**: неизмеренный
  раннер попадает в класс FR-09 и отвергается до платного вызова, а не
  превращается в задачу, которая не завершится никогда.
- **CON-05** `verify_red` сводит любую причину неподтверждения к одному
  значению `UNVERIFIABLE`, сохраняя лишь текст сообщения: восемь собственных
  ранних возвратов (`tdd.py:288`, `:302`, `:317`, `:332`, `:349`, `:365`,
  `:373`, `:378`) плюс один из `_classify` (`:455`). Причина до вызывающего
  не доходит — поэтому доказуемость различения входит в FR-04 как часть
  требования, а конкретное отображение причин на вердикты принадлежит
  design.
- **CON-06** Без `auto_commit` кандидат-коммита нет (FR-09).

## 7. Вне объёма

- **OUT-01** Правка репозитория devtools.
- **OUT-02** Новые классы waiver'а.
- **OUT-03** Ретроспективная проверка TASK-008/009/011.
- **OUT-04** Автоматический вывод мутанта или селектора.
- **OUT-05** Распространение на `Mode: tdd` и `verify_first`.
- **OUT-06** Уровень version bump (Q-C).

## 8. Метрики успеха

- **M-01** Доля waived-задач класса `characterisation`, завершившихся без
  исполненного контроля: сегодня 100 % (три из трёх), цель — 0 %.
- **M-02** Число мест, где условие negative control существует только как
  текст: сегодня два (пункт чек-листа, абзац ревьюеру), цель — ноль как
  **единственных** носителей; текст остаётся, но поверх исполнения.
- **M-03** Число git-вызовов и открытий state DB у задачи без waiver'а:
  сегодня N, цель — ровно N.

## 9. Трассировка

### FR/NFR → цели, персоны, метрики

| Требование | Цели | Персоны | Метрики |
|---|---|---|---|
| FR-01 | G-01 | P-03 | M-01 |
| FR-02, FR-03, FR-04, FR-05 | G-01 | P-01, P-03 | M-01 |
| FR-06 | G-02 | P-01, P-02 | M-02 |
| FR-07 | G-01 | P-01 | M-01 |
| FR-08 | G-03 | P-02 | M-02 |
| FR-09 | G-01 | P-01 | — |
| FR-10 | G-01 | P-03 | M-01 |
| FR-11 | G-02 | P-04 | — |
| FR-12 | — | P-03 | — |
| NFR-02 | G-04 | P-01 | M-03 |

### Требования → критерии приёмки чартера

| Критерий чартера | Требования |
|---|---|
| Корректный контроль исполнен, задача завершилась | FR-01…FR-07 |
| Задача без объявления отказана | FR-01, FR-07, FR-10 |
| Патч не роняет тест | FR-03 |
| Патч роняет сбор | FR-03, FR-04 |
| Тест красный без мутанта | FR-05 |
| Таймаут на мутированной половине | FR-04 |
| Неприменимый патч | FR-05 |
| `auto_commit: false` | FR-09 |
| Задача без waiver'а не платит | NFR-02 |
| Пояс не обойдён | NFR-01 |
| `tdd status` и `--json` — одно свидетельство | FR-06 |
| Текст обязательства переписан | FR-08 |
| Документация и закрытие | FR-11 |

### Риски → требования-меры

| Риск | Мера |
|---|---|
| RK-01 патч ломает не то | FR-08 (и чтение патча глазами в ревью) |
| RK-02 мутант роняет сбор | FR-03 (`PROVEN` обязателен) |
| RK-03 зелёный оригинал как формальность | FR-05 (вердикты чистой половины) |
| RK-04 утечка worktree | NFR-03 |
| RK-05 патч не к тому дереву | FR-02 (один коммит на обе стороны) |
| RK-06 расползание на класс | OUT-02 |
| Сужение → потеря внимания ревьюера | FR-08 как Must |

## 10. Открытые вопросы

Блокирующих нет: Q-A и Q-B закрыты решением владельца 2026-09-21 (AP-10).

- **Q-C · product · blocking: false** — уровень version bump. `AGENTS.md:18`
  требует major при любом изменении SQLite state surface; v2.36.0 от
  2026-09-15 добавил две таблицы и вышел минором с письменным обоснованием.
  Решение нужно до релиза, не до реализации.
- **Q-D · architect · blocking: false** — нужен ли свидетельству собственный
  `config_hash`-пин, чтобы оно не пережило смену адаптера. *Предложение:*
  нужен, по основаниям #198.
- **Q-E · product · blocking: false** — три закрытые задачи. *Предложение:*
  не трогать (OUT-03).
- **Q-G · architect · blocking: false — селектор и сдвиг строк, поднято
  финальным прогоном.** На раннере со строковым селектором (ExUnit,
  `path:line`) корректный мутант благословлённой формы — «инвертировать
  assertion в самом тесте», которую прямо называет действующий текст
  обязательства (`config.py:102`), — может сдвинуть строки тест-файла.
  Тогда объявленный селектор на мутированной половине указывает не туда:
  `preflight` отвечает `not_a_definition_line`, а при неудачном совпадении
  `prove_selected` вернёт `REFUTED` по чужому тесту. По FR-04 исход
  прочитается как вина патча, и **корректный мутант никогда не даст
  удовлетворённого контроля**. Починить вторым селектором нельзя: FR-01
  фиксирует «один на обе половины» как Must.
  *Предложение:* объявить ограничение — на раннерах со строковым селектором
  патч обязан сохранять позицию объявления теста, — и дать этому исходу
  собственную названную причину («селектор недействителен после патча»),
  отличную от «мутант не различил». Разрешается в design, где живёт
  адресация селекторов; на pytest (node id) вопрос не возникает.
- **Q-F · product · blocking: false** — классификация неприменимого патча:
  instrument error (AP-02 буквально) или «не удовлетворён» без ретраев
  (применимость детерминирована). *Предложение:* различать «файла нет или
  нечитаем» — instrument, «не применяется к своему же коммиту» — не
  удовлетворён. Противопоставление AP-02 с УСПЕХОМ верно при любом ответе.

  Подслучай «патча **нет в кандидат-коммите**» вопросом больше не является
  и решён здесь **допущением**, чтобы стадия не стояла (круги 6 и 8): это
  не сбой применения, а отсутствие продукта задачи — детерминированный
  факт о работе, и единственный механизм, которым FR-10 обеспечивает свой
  «блокирующий факт». Оставить его instrument error значило бы нарушить
  инвариант 4 и оставить FR-10 без опоры.

  **Допущение (ожидает подтверждения владельца, работу не блокирует):**
  три случая врозь — **нет в коммите** → не удовлетворён, без ретраев;
  **файл нечитаем (I/O)** → instrument; **`git apply` отказал** → предмет
  Q-F, записан по AP-02 буквально. Первые два не спорят с AP-02: там речь
  об ошибке ПРИМЕНЕНИЯ, а не об отсутствии файла. Если владелец решит
  иначе, меняется один пункт FR-05 и одна строка FR-10.

## 11. Условие завершения стадии

Стадия закончена, когда каждое требование чартера раскрыто в FR/NFR с
приёмкой, проверяемой наблюдением; трассировка покрывает все критерии
приёмки чартера; блокирующих вопросов нет; локальное ревью не даёт
blocker/major находок.

## Источники

- `00-charter.md` (эта же ветка) — цели, объём, инварианты, критерии
  приёмки.
- `00-discovery/brief.md` — S-01…S-07, CON, AP-01…AP-10, RK, Q.
- Решения владельца 2026-09-21: форма мутанта и объявления, порядок половин,
  fail-closed, ответственность ревьюера, процесс бандлом.
- Код на `8feff9b`: `tdd.py:270`, `:289`–`:378`, `:447`, `:456`, `:855`;
  `gates.py:414`, `:584`; `execution.py:584`, `:656`; `hooks.py:1166`,
  `:1176`; `config.py:96`; `tdd_status.py:28`.
