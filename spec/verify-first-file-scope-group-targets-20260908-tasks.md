---
traces_to:
- decomposition
upstream_hashes:
  decomposition: 6cc1598fb874c0c9d6b7a5f39981a56bf9967257
spec_stage: tasks
status: approved
version: 5
generated_by: fleet-agent
generated_at: '2026-09-12T08:40:15+04:00'
source_prompt_version: ''
validation: pass
approved_by: andrei-shtanakov
approved_at: '2026-09-12T05:07:46Z'
owner_role: stream-owner
---

## Milestone 1: Verify-first file-scope group targets

Сгенерировано task_bridge из behaviour-spec бандла verify-first-file-scope-group-targets-20260908 (шаг 3 плана развития конвейера; группировка задач — по Feature-секциям). Draft: исполнение только после человеческого approve.

## Решения открытых вопросов (уровень design)

**Q-01 — resolved:**

**Состав разрешает сам раннер, в том же одном прогоне, через собственный
репортёр адаптера; граница «раннер сказал / мы вывели» проходит по двум
раздельным фазам этого прогона — сбору и отчёту об исходах.**

Продуктовая рамка требует одновременно: один запуск раннера на элемент (FR-06,
BEH-08), разрешение против судимого коммита в среде прогона (FR-05), пер-членную
конъюнкцию (FR-08) и **обнаружимость молчания о члене** с указанием его имени
(FR-10, BEH-15). Последнее — решающее ограничение. Если «состав» определить как
перечень членов, о которых высказался отчёт об исходах, то неучтённый член
невозможен по построению, и FR-10 становится пустым правилом. Значит, состав и
пер-членные исходы обязаны иметь **два разных источника внутри одного процесса
раннера**: фазу сбора (какие члены существуют в этом файле на этом коммите) и
фазу отчёта (что с каждым из них случилось). Разность двух множеств и есть
множество неучтённых — прогон, оборвавшийся на середине, назван поимённо, а не
прочитан как «всё, что я вижу, зелёное».

Получить имена собранных членов из прогона, который эти члены ещё и исполняет,
можно только попросив об этом сам раннер. Поэтому механизм — **репортёр,
принадлежащий адаптеру и внедряемый в прогон адаптером** (для pytest —
плагин-сайдкар, включаемый флагом инвокации; см. «Механика»), который пишет
машиночитаемый манифест: одну запись на каждый собранный член в момент
завершения сбора и одну запись на каждый объявленный исход. Это ровно тот
принцип, которым уже живут `_DEFINITION_LINES_SCRIPT` (об Elixir спрашивают
Elixir, а не учат Python читать Elixir) и `PytestAdapter.classify` (коды
измерены, фолбэка на «код возврата» нет по построению). Отсюда же следует, что
**невозможность установить репортёр — это `instrument_error`, а не повод
прочитать терминальную сводку**: сводочный фолбэк и есть дефект #198/#201 на
другой гранулярности («1 failed, 97 passed» как доказательство про один тест).

Владеет всем этим адаптер (FR-03): и словарём («что вообще является файловой
целью для этого раннера»), и разрешением состава, и чтением манифеста. Общая
часть — `live_verify` — не знает ни про pytest, ни про формат манифеста; она
получает от адаптера уже готовый пер-членный отчёт и сводит его в те же три оси,
что и сегодня (см. Q-03).

**Q-02 — resolved:**

**Новой оси не вводится: две уже действующие оси покрывают дрейф состава строже,
чем любая ось «по составу», которую можно было бы добавить. Добавляется только
видимость — состав пишется в эвиденцию.**

FR-15 требует, чтобы зелёное не наследовалось деревом, в котором состав мог
измениться, и прямо называет недостаточной байт-идентичность объявленной строки.
Разбор существующих осей:

- **До прогона** (`reusable_verify_evidence`) переиспользование уже требует
  **байт-идентичности дерева всего репозитория** (`_tree_hash` обеих сторон,
  причём нечитаемое дерево с любой стороны — это «не знаю», а не «совпало»).
  Дерево, в которое добавили тест, байт-идентичным не является. Ось «по составу»
  здесь ничего не добавила бы: она слабее той, что уже стоит.
- **Перед мержем** гейт `_verify_first_gate` читает эвиденцию, а её актуальность
  обеспечивают два повторных живых прогона (`_reverify_before_review` и
  `_reverify_live_evidence_for_candidate`): если HEAD сдвинулся, группа
  прогоняется заново на кандидате, и состав разрешается заново против него же.
  То есть на этом пути состав вообще не наследуется — он перевычисляется.
- **Между входным прогоном и кандидатом** объявленный файл заморожен байт-локом
  (`record_verify_group_claims`, FR-18): добавленный в файл тест — это изменение
  байтов заявленного пути, то есть нарушение claims с сегодняшним сообщением.

Что действительно нужно добавить — не ось решения, а **предъявление**: состав,
который судили, записывается в эвиденцию (FR-13), чтобы читатель мог проверить
вердикт, а не принимать его на веру. Одно точечное изменение всё же требуется в
`record_verify_group_claims`: он парсит селекторы через `parse_selector` и без
правки не сможет заморозить файловую цель — см. «Механика».

Осознанная цена, называемая явно: сегодняшняя консервативность
`reusable_verify_evidence` сохраняется без ужесточения (BEH-22, последний
пункт) — дерево, в котором ничего не менялось, переиспользуется как и сегодня.

**Q-03 — resolved:**

**Исчерпывающее отображение живёт в одной чистой функции-свёртке
`live_verify`, стоящей ПЕРЕД `classify_verify_outcome`, а не вместо него.
`classify_verify_outcome` остаётся единственной точкой вердикта и не меняется ни
одной строкой.**

Свёртка принимает пер-членный отчёт (результат Q-01) и возвращает **тот же
триплет**, который классификатор уже умеет судить: `RunOutcome`,
`SelectionProof`, `ExecutionProof`. Отображение:

| Состав и пер-членные факты | Триплет | Исход |
|---|---|---|
| состав пуст | `SELECTION_FAILED`, `UNKNOWN`, `NOT_EXECUTED` | `instrument_error` |
| хотя бы один член не учтён | `TESTS_PASSED`/как есть, `UNKNOWN`, — | `instrument_error` |
| все учтены, есть упавший или ошибочный | `TESTS_FAILED`, `PROVEN`, — | `test_failure` |
| все учтены, падений нет, исполнен ноль членов | `TESTS_PASSED`, `PROVEN`, `NOT_EXECUTED` | `instrument_error` |
| все учтены, падений нет, исполнен ≥1 и прошёл | `TESTS_PASSED`, `PROVEN`, `EXECUTED` | `green` |
| отчёт не прочитан / репортёр не установлен | `UNRECOGNIZED`, `UNKNOWN`, `UNDETERMINED` | `instrument_error` |

Три следствия, ради которых выбрана именно такая форма:

1. **Четвёртого исхода не появляется по построению** (FR-17, BEH-24): свёртка
   физически не может вернуть ничего, кроме триплета, а классификатор тотален
   над триплетами и всё неназванное сводит к `instrument_error`.
2. **Объявленная асимметрия локализована в одном месте.** Правило учтённости
   (пропуск не мешает зелёному) существует ровно в свёртке; правило node id
   («пропуск — не исполнение») продолжает жить там, где живёт сегодня — в
   `execution_proven` и прямом вызове классификатора. Ветка node id не проходит
   через свёртку вовсе, поэтому FR-04/BEH-06 выполняется не обещанием, а
   отсутствием общего кода.
3. **Таблица покрывается одним параметризованным тестом** (BEH-12): свёртка
   чистая, её вход — структура, а не `CompletedProcess`, поэтому строки таблицы
   задаются данными, а не фикстурными репозиториями.

Приоритет строк фиксируется явно: **неучтённость проверяется раньше падения**.
Отчёт, в котором один член упал, а о другом не сказано ничего, — это
`instrument_error`, а не `test_failure`: инструмент не доказал, что рассказал обо
всём составе, и «настоящее падение» из FR-11 относится к составу, о котором
прогон высказался целиком.

**Q-04 — resolved:**

**Аддитивное поле рядом. `group_declared` и `group_executed` не меняют ни тип,
ни смысл; состав едет в новом nullable-поле `composition`.**

Почему не расширение `group_executed`: его сегодняшний смысл — «элементы
объявления, реально предъявленные адаптеру, в порядке предъявления»; именно на
этом смысле стоит наблюдаемое «группа, остановившаяся на втором селекторе, имеет
executed длины 1». Положить туда node id членов файла — это молча сменить смысл
существующего поля (прямо против NFR-04 и FR-16) и заодно потерять факт «второй
элемент не запускался».

Форма: колонка `composition TEXT` в таблице `verify_evidence` (`ADD COLUMN`,
аддитивная миграция; пустое значение = «строка записана до этого изменения», и
это не дефект — FR-16, BEH-23). Содержимое — JSON: список записей по **элементам
объявления, являющимся файловыми целями**, каждая запись несёт объявленный
элемент вербатим, размер состава и упорядоченный перечень членов с их учтённым
итогом и причиной неисполнения. Соответствующее поле на `VerifyEvidence`
объявляется со значением по умолчанию, чтобы старые строки конструировались без
него.

Отдельное решение, важное для FR-04: **группа из одних node id не пишет в
`composition` ничего**. Её строка эвиденции остаётся байт-идентичной сегодняшней,
и «эвиденция для группы из node id не меняется» выполняется буквально, а не
приблизительно.

Внешние поверхности расширяются тем же аддитивным правилом, но поверхностей
две, и устроены они по-разному.

Со стороны состояния расширяется **каноническая SQLite-схема**: `composition`
добавляется колонкой таблицы `verify_evidence` (`ALTER TABLE ... ADD COLUMN`), и
база, заведённая до этого поля, читается с пустым составом, а не как дефект
(BEH-23). `schemas/executor-state.schema.json` при этом не трогается вовсе — и
это не упущение и **не** признак того, что схема мертва. Схема жива: она
описывает содержимое `.executor-state.db`, и её словари прибиты к продуктовому
коду живыми тестами — `error_code` сверяется с `ErrorCode`, `error_kind` с
`ERROR_KINDS`, а `error_stage` получил значение `verify` в v2.35.0, когда
предыдущий workstream изменил описываемую ею поверхность. Расширяют её ровно
тогда, когда меняется то, что она описывает, — и наш состав в это «то» не
входит: схема моделирует документ состояния (задачи с их попытками и счётчики),
а таблицу `verify_evidence` не описывает вовсе, поэтому месту для `composition`
в ней взяться неоткуда.

Со стороны `--json-result` поверхность расширяется — и уже расширена: для
FR-21/FR-22 необязательная сводка (размер состава / сколько исполнено / сколько
пропущено) отдаётся рядом с уже отдаваемым `verify_outcome` из
`build_task_json_result` и описана в `schemas/json-result.schema.json`.

Golden-фикстуры Maestro-интеропа не перегенерируются: потребитель, не знающий
новых полей, читает те же значения (BEH-30).

**Q-05 — resolved:**

**ExUnit в этом workstream файловую цель НЕ получает и отказывает по имени через
механизм FR-03.**

Основание — измеренная цена ошибки на этом самом адаптере: коды возврата ExUnit
инвертированы относительно pytest, и однажды это уже стоило ложного
подтверждённого красного (#198), после чего появилось правило «исход считается
только для раннера, чьи коды измерены» (`MEASURED_RUNNERS`). Пер-членная
учтённость требует от ExUnit того же класса измерений заново — какой формой
`mix test` перечисляет собранные тесты, что он печатает про `@tag :skip`, и как
это отличить от прерванного прогона. Вся приёмка этого workstream (NFR-01,
BEH-08, BEH-32) стоит на pytest; принимать ExUnit «заодно», без собственной
измеренной матрицы, — это ровно тот шаг, который #198 запретил.

Побочная выгода, делающая решение самопроверяемым: BEH-05 требует адаптера
**без** поддержки как входа. С этой резолюцией такой адаптер — настоящий, а не
синтетический, и «поддержка принадлежит адаптеру» доказывается двумя реальными
адаптерами, а не моком.

Форма отказа — объявленная возможность адаптера, а не отсутствие метода:
адаптер, не объявивший поддержку файловых целей, отказывает на `validate`
сообщением, называющим адаптер и форму, и до прогона не доходит (BEH-05).
Расширение ExUnit — отдельная работа со своей измеренной матрицей.


## Критерии приёмки (уровень acceptance)

- **AC-01** (test): Файловая цель принята объявлением в обеих формах и в объявленном порядке
- **AC-02** (test): Словарь конечен: всё, что матчит «что-нибудь», отказывает по имени
- **AC-03** (test): Поддержка файловой цели принадлежит адаптеру и её отсутствие объявлено
- **AC-04** (test): Ветка объявления перечнем node id не сдвинулась ни на одном наблюдаемом
- **AC-05** (test): Судится состав судимого коммита, а не рабочего дерева
- **AC-06** (test): Один элемент объявления — один запуск раннера
- **AC-07** (test): `validate` отказывает ровно на том, что решается без прогона
- **AC-08** (test): Зелёное — конъюнкция пер-членных фактов, код возврата не доказывает ничего
- **AC-09** (test): Пустой и полностью неисполнившийся состав — инструментальная ошибка
- **AC-10** (test): Молчание о члене — отказ; пропуск с названной причиной — учёт
- **AC-11** (test): Упавший член — настоящее падение; исходов по-прежнему ровно три
- **AC-12** (test): Смешанная группа судится как объявлена, включая пересечение и дубль
- **AC-13** (test): Эвиденция называет разрешённый состав поимённо и с пер-членным итогом
- **AC-14** (test): Объявленное хранится вербатим
- **AC-15** (test): Вердикт не наследуется деревом, в котором состав мог измениться
- **AC-16** (test): Существующая эвиденция читается и трактуется как прежде
- **AC-17** (test): Байт-лок покрывает тот файл, состав которого судился
- **AC-18** (test): Пред-мерж гейт задаёт тот же вопрос в том же месте
- **AC-19** (test): Отказ называет файл и причину и читается там же, где сегодня
- **AC-20** (test): Размер состава предъявлен по ходу прогона, и нового потолка не введено
- **AC-21** (test): Зелёное с пропусками отличимо от полностью исполненного
- **AC-22** (test): Ни один класс объявления, разрешения и отказа не совершает платного вызова
- **AC-23** (test): Внешние контракты расширены только аддитивно
- **AC-24** (test): Обе ветки покрыты регрессионно, и покрытие перечислено, а не заявлено
- **AC-25** (test): Один и тот же вход даёт один и тот же состав и один и тот же исход
- **AC-26** (metric): Замеренное сокращение воспроизведено замером, а не оценкой
- **AC-27** (metric): Замеренная выборка целей воркспейса объявляема без ручного расширения
- **AC-28** (manual): Объявленная граница снята из документации и заменена описанным контрактом
- **AC-29** (manual): Принятая форма объявлена владельцу конвейера без правки его репозитория

### TASK-001: Вокабуляра объявленной группы и локатор файловой цели
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-03 (DT-01, группа core).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-01

**Checklist:**
- [x] реализовать BEH-03: Словарь остаётся конечным: всё, что матчит «что-нибудь», отказывает по имени
- [x] проверка группы: tests/test_verify_file_target_dictionary.py (kind: contract) зелёные на BEH-03

**Traces to:** [FR-02]

### TASK-002: Инвокация файловой цели: preflight по судимому дереву, репортёр адаптера, манифест состава
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-07 (DT-02, группа core).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-02
**Depends on:** [TASK-001]

**Checklist:**
- [x] реализовать BEH-07: Состав разрешается против судимого коммита, а не против рабочего дерева
- [x] проверка группы: tests/test_verify_file_composition.py (kind: integration) зелёные на BEH-07

**Traces to:** [FR-05]

### TASK-003: Свёртка пер-членного отчёта в триплет и исходы файловой цели
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-10, BEH-11, BEH-12, BEH-13, BEH-14, BEH-15, BEH-16 (DT-03, группа core).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-03
**Depends on:** [TASK-002]

**Checklist:**
- [x] реализовать BEH-10: Отказ по файловой цели наступает до первого платного вызова
- [x] реализовать BEH-11: «Прогон вернул 0» доказательством не является
- [x] реализовать BEH-12: Зелёное по файловой цели — конъюнкция пер-членных фактов
- [x] реализовать BEH-13: Пустой собранный состав — `instrument_error`, называющий файл
- [x] реализовать BEH-14: Состав, ни один член которого не исполнился, — не здоровье
- [x] реализовать BEH-15: Молчание о члене состава — отказ, а не зачёт
- [x] реализовать BEH-16: Частично пропущенный файл зелёный, и пропуски названы поимённо
- [x] проверка группы: tests/test_verify_file_target_outcomes.py (kind: integration), tests/test_verify_file_target_outcomes.py (kind: contract) зелёные на BEH-10, BEH-11, BEH-12, BEH-13, BEH-14, BEH-15, BEH-16

**Traces to:** [FR-07], [FR-09], [FR-08], [FR-10]

### TASK-004: `validate`: разбор объявления вокабулярой группы и граница ошибки и предупреждения
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-09, BEH-09a (DT-04, группа core).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-04
**Depends on:** [TASK-001]

**Checklist:**
- [x] реализовать BEH-09: `validate` отказывает ровно на том, что решается без прогона
- [x] реализовать BEH-09a: Снятая граница отставлена и в эвиденциальном red-артефакте #367/TASK-003 (`tests/test_task_003_55403b38f9226be0_14df0641_red.py`)
- [x] проверка группы: tests/test_verify_first_validate.py (kind: integration), tests/test_task_003_55403b38f9226be0_14df0641_red.py (kind: integration) зелёные на BEH-09, BEH-09a

**Traces to:** [FR-07]

### TASK-005: Байт-лок файловой цели в `record_verify_group_claims`
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-25 (DT-05, группа state-and-surfaces).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-05
**Depends on:** [TASK-001]

**Checklist:**
- [x] реализовать BEH-25: Байт-лок покрывает тот файл, состав которого судился
- [x] проверка группы: tests/test_verify_claims.py (kind: integration) зелёные на BEH-25

**Traces to:** [FR-18]

### TASK-006: Эвиденция состава: аддитивная колонка, запись и чтение старых строк
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-20, BEH-21, BEH-23 (DT-06, группа state-and-surfaces).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-06
**Depends on:** [TASK-003]

**Checklist:**
- [x] реализовать BEH-20: Эвиденция называет состав поимённо и с пер-членным итогом
- [x] реализовать BEH-21: Объявленное хранится вербатим
- [x] реализовать BEH-23: Существующая эвиденция остаётся читаемой
- [x] проверка группы: tests/test_verify_evidence.py (kind: contract) зелёные на BEH-20, BEH-21, BEH-23

**Traces to:** [FR-13], [FR-14], [FR-16]

### TASK-007: Поверхности оператора: размер состава, «зелено с пропусками», сводка в `--json-result`
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-27, BEH-28, BEH-29 (DT-07, группа state-and-surfaces).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-07
**Depends on:** [TASK-003], [TASK-004], [TASK-006]

**Checklist:**
- [x] реализовать BEH-27: Каждый отказ называет файл и причину и читается без логов
- [x] реализовать BEH-28: Размер разрешённого состава предъявляется по ходу прогона и записывается
- [x] реализовать BEH-29: Зелёное с пропусками отличимо от полностью исполненного
- [x] проверка группы: tests/test_verify_cli.py (kind: integration) зелёные на BEH-27, BEH-28, BEH-29

**Traces to:** [FR-20], [FR-21], [FR-22]

### TASK-008: Ветвление на файловой цели: упавший член и три исхода
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-17, BEH-24 (DT-08, группа regression).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-08
**Mode:** standard
**TDD-waiver:** characterisation · sanction: batch-approve-2026-09-09
**Depends on:** [TASK-003]

**Checklist:**
- [x] TDD-waiver DT-08, класс characterisation (санкция batch-approve-2026-09-09) — подтвердить ВСЕ условия класса: поведение уже доставлено зависимостями задачи; задача добавляет отсутствующее characterisation/acceptance-покрытие; честный baseline RED невозможен; новый тест имеет negative control, доказывающий, что он краснеет при нарушении свойства; baseline-sha зафиксирован фактический на старте задачи
- [x] реализовать BEH-17: Упавший член — настоящее падение, а не проблема инструмента
- [x] реализовать BEH-24: Три исхода и ветвление по ним не меняются
- [x] проверка группы: tests/test_verify_file_target_branching.py (kind: integration), tests/test_verify_file_target_branching.py (kind: contract) зелёные на BEH-17, BEH-24

**Traces to:** [FR-11], [FR-17]

### TASK-009: Гейт и переиспользование эвиденции на файловой цели
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-22, BEH-26 (DT-09, группа regression).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-09
**Mode:** standard
**TDD-waiver:** characterisation · sanction: batch-approve-2026-09-09
**Depends on:** [TASK-006]

**Checklist:**
- [x] TDD-waiver DT-09, класс characterisation (санкция batch-approve-2026-09-09) — подтвердить ВСЕ условия класса: поведение уже доставлено зависимостями задачи; задача добавляет отсутствующее characterisation/acceptance-покрытие; честный baseline RED невозможен; новый тест имеет negative control, доказывающий, что он краснеет при нарушении свойства; baseline-sha зафиксирован фактический на старте задачи
- [x] реализовать BEH-22: Вердикт не наследуется деревом, в котором состав мог измениться
- [x] реализовать BEH-26: Пред-мерж гейт задаёт тот же вопрос
- [x] проверка группы: tests/test_verify_file_target_gates.py (kind: integration) зелёные на BEH-22, BEH-26

**Traces to:** [FR-15], [FR-19]

### TASK-010: Приёмка объявления снаружи: обе формы, смешанная группа, дубль, отсутствие вывода группы
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-01, BEH-02, BEH-04, BEH-18, BEH-19 (DT-10, группа regression).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-10
**Depends on:** [TASK-002], [TASK-003], [TASK-004]

**Checklist:**
- [x] реализовать BEH-01: Файловая цель объявляется наравне с node id, в обеих формах и в любом порядке
- [x] реализовать BEH-02: Замеренная выборка целей воркспейса объявляется без ручного расширения
- [x] реализовать BEH-04: Группа не выводится ни из чего, кроме объявления
- [x] реализовать BEH-18: Смешанная группа судится как объявлена, включая пересечение
- [x] реализовать BEH-19: Тест, попавший в состав дважды, удовлетворяет правило оба раза
- [x] проверка группы: tests/test_verify_file_target_declaration.py (kind: contract), tests/test_verify_file_target_declaration.py (kind: e2e), tests/test_verify_file_target_declaration.py (kind: integration) зелёные на BEH-01, BEH-02, BEH-04, BEH-18, BEH-19

**Traces to:** [FR-01], [FR-02], [FR-12]

### TASK-011: Объявленная граница адаптера: два адаптера на одном объявлении
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-05 (DT-11, группа regression).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-11
**Mode:** standard
**TDD-waiver:** characterisation · sanction: batch-approve-2026-09-09
**Depends on:** [TASK-002], [TASK-004]

**Checklist:**
- [x] TDD-waiver DT-11, класс characterisation (санкция batch-approve-2026-09-09) — подтвердить ВСЕ условия класса: поведение уже доставлено зависимостями задачи; задача добавляет отсутствующее characterisation/acceptance-покрытие; честный baseline RED невозможен; новый тест имеет negative control, доказывающий, что он краснеет при нарушении свойства; baseline-sha зафиксирован фактический на старте задачи
- [x] реализовать BEH-05: Поддержка файловой цели принадлежит адаптеру, и её отсутствие объявляется по имени
- [x] проверка группы: tests/test_verify_file_target_adapter.py (kind: contract) зелёные на BEH-05

**Traces to:** [FR-03]

### TASK-012: Аддитивность внешних контрактов на составе файловой цели
P2 | ✅ DONE   Est: 0.5d

Реализовать сценарии BEH-30, BEH-33 (DT-12, группа regression).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-12
**Depends on:** [TASK-007]

**Checklist:**
- [x] реализовать BEH-30: Внешние контракты расширяются только аддитивно
- [x] реализовать BEH-33: Перечень допустимых полей `--json-result` знает `verify_composition` (`OPTIONAL_TASK_RESULT_FIELDS` в `tests/test_json_result_contract.py`)
- [x] проверка группы: tests/test_verify_file_target_contracts.py (kind: contract) зелёные на BEH-30, BEH-33

**Traces to:** [FR-13], [FR-16]

### TASK-013: Замер стоимости класса и харнесс-гарантия
P2 | TODO   Est: 0.5d

Реализовать сценарии BEH-08, BEH-32 (DT-13, группа regression).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-13
**Depends on:** [TASK-003]

**Checklist:**
- [ ] реализовать BEH-08: Один элемент объявления — один запуск раннера
- [ ] реализовать BEH-32: Стоимость класса измерена, и ни один тест не вызывает реального агента
- [ ] проверка группы: tests/test_verify_file_target_cost.py (kind: e2e) зелёные на BEH-08, BEH-32

**Traces to:** [FR-06], [FR-09]

### TASK-014: Регрессия ветки node id
P2 | TODO   Est: 0.5d

Проверить сценарии BEH-06 (DT-14, группа regression).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-14
**Mode:** verify_first
**Verifies:** tests/test_verify_outcomes.py, tests/test_verify_first_declaration.py, tests/test_verify_first_validate.py, tests/test_verify_claims.py, tests/test_verify_evidence.py, tests/test_verify_file_target_outcomes.py
**Depends on:** [TASK-005], [TASK-007]

**Checklist:**
- [ ] проверить BEH-06: Группа из одних node id ведёт себя в точности как сегодня
- [ ] проверка группы: tests/test_verify_outcomes.py (kind: contract) зелёные на BEH-06

**Traces to:** [FR-04]

### TASK-015: Снятие объявленной границы из документации и описание принятого контракта
P2 | TODO   Est: 0.5d

Реализовать сценарии BEH-31 (DT-15, группа solo).
Source: workstreams/verify-first-file-scope-group-targets-20260908/spec/30-decomposition.md#DT-15
**Depends on:** [TASK-005], [TASK-007]

**Checklist:**
- [ ] реализовать BEH-31: Объявленная граница снимается и заменяется описанным контрактом
- [ ] проверка группы: docs/architecture.md (kind: manual) зелёные на BEH-31

**Traces to:** [FR-01], [FR-04], [FR-10]

